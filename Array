/**
 * This Java program counts the occurrences of the number `1` in an integer array.
 * It iterates through the array and increments a counter each time it finds a `1`.
 * Finally, it prints the total count.
 */
class Main {
    public static void main(String[] args) {
        // Define an integer array with some values
        int arr[] = {10, 1, 1, 1, 1, 4, 6, 5};

        // Variable to store the count of occurrences of '1'
        int count = 0;

        // Loop through the array to count occurrences of '1'
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == 1) {
                count += 1; // Increment count when '1' is found
            }
        }

        // Print the final count of '1' in the array
        System.out.println("Count of 1s in the array: " + count);
    }
}


/**
 * This Java program counts the number of even numbers in an integer array.
 * It iterates through the array and increments a counter whenever an even number is found.
 * Finally, it prints the total count of even numbers.
 */
class Main {
    public static void main(String[] args) {
        // Define an integer array with some values
        int arr[] = {10, 1, 1, 1, 1, 4, 6, 5};

        // Variable to store the count of even numbers
        int count = 0;

        // Loop through the array to count even numbers
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] % 2 == 0) { // Check if the number is even
                count += 1; // Increment count for even numbers
            }
        }

        // Print the final count of even numbers in the array
        System.out.println("Count of even numbers in the array: " + count);
    }
}
